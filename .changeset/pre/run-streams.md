---
"@nestm/ai-sdk": patch
---

Add `/runs` with a typed process-local run stream registry: bounded reservations, readiness, reconnectable subscriptions, host-owned replay projections, explicit cancellation and shutdown. No Harness, persistence or additional dependencies are introduced.
