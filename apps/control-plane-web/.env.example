# Optional server-side source. When omitted, the private dashboard serves its
# bundled content-free demo snapshot.
AI_OBSERVABILITY_API_URL=http://127.0.0.1:3001
# Optional server-only credential for a protected snapshot endpoint.
# AI_OBSERVABILITY_API_BEARER_TOKEN=replace-me
